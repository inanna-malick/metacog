{
  "name": "metacog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "build:local": "node esbuild.config.mjs",
    "start:local": "node dist/stdio.mjs",
    "docker:build": "docker build -t metacog-mcp .",
    "docker:run": "docker run -i --rm metacog-mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.23.0",
    "agents": "^0.2.4",
    "workers-mcp": "^0.0.13",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250926.0",
    "esbuild": "^0.25.0",
    "prettier": "^3.6.2",
    "typescript": "5.9.2",
    "wrangler": "^4.40.1"
  }
}
